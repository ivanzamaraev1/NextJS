{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next` start",
    "mock": "json-server -w db.json -p 4200 -d 1000",
    "serve": "concurrently \"npm run dev\" \"npm run mock\""
  },
  "dependencies": {
    "next": "10.0.9",
    "nextjs-progressbar": "^0.0.8",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "sass": "^1.32.8"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}
